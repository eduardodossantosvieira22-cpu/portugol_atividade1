# portugol_atividade1
. O codigo inicia e com o comando escreva, que escreve na tela a frase "Ola mundo!".

2. O codigo é quase o mesmo da primeira, com a diferença de que é adicionado um segundo comando escreva, que escreve na tela "Como vai".
 
3. O codigo é muito semelhante ao segundo, porém, após o "Ola mundo!", vem o "\n", que indica que tudo que for escrito na tela de ali em diante sera na linha de baixo.
 
4. O codigo começa definindo uma a variavel cadeia, que é usada para palavras, a variavel em questão é "nome", após isso é usado o comando leia, que o usuario digite uma resposta, e atribui "nome" a essa resposta, e então um comando escreva que escreve na tela a variavel "nome", que agora é a resposta do usuario.
   
5. O codigo tem três comandos escreva, com o primeiro tendo o numro 123 fora de parenteses, seguido do "\n", sendo assim ele é apenas escrito na tela, no segundo escreva, temos a soma 100+200, mais uma vez fora dos paeanteses, seguido de mais um "\n", desta forma, quando o codigo é executado, a soma é realizada normalmente, ja no terceiro escreva temos o mesmo calculo seguido de um "\n", porém desta vez a soma esta entre parenteses, assim sendo considerada texto, pois tudo aquilo que estver entre parenteses na função escreva é considerado texto. Sendo assim a tela final após o codigo ser executado é: na primeira linha onumero 123, na segunda o numero 300 e na terceira a soma 100+200.
 
6. O codigo inicia e define o variavel "inteiro", que é semelhante a variavel "escreva", pórem para numeros inteiros, e o define para idade, que igual a 21, então no comando se, que realiza algo apenas se uma informação for verdadeira, é posto que se a variavel idade for maior ou igual 18, sera escrito o texto "Você é maior de idade", e como esta definido que a variavel idade é igual a 21, sera verdadeiro.
